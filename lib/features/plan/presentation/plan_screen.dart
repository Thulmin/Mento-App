// Keeps the main Plan navigation destination focused on the academic organiser.

import 'package:flutter/material.dart';

import '../../academic_organizer/presentation/academic_organizer_screen.dart';

class PlanScreen extends StatelessWidget {
  const PlanScreen({super.key});

  @override
  Widget build(BuildContext context) => const AcademicOrganizerScreen();
}
